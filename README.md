## Double Ended Circular Queue Benchmark

Clone the repo

1. run `npm install`
2. run `npm start`

The circular queue is right now a fixed size but can be made dynamic with little change.

PRs are welcome.